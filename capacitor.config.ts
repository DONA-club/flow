import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.stellarocelotwiggle',
  appName: 'stellar-ocelot-wiggle',
  webDir: 'dist'
};

export default config;
